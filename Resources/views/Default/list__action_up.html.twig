{% block field %}
    {% spaceless %}
		{% if object.parent is not null and object.parent.children[0].id != object.id %}
		<a class="btn btn-sm btn-default" href="{{ admin.generateObjectUrl('up', object) }}">
            &uarr;
        </a>
		{% endif %}
    {% endspaceless %}
{% endblock %}
